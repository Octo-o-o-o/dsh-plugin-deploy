<!-- generated by tools/gen-facts.mjs — DO NOT EDIT BY HAND
     source: DeepSeek Harness @ 99f6f02fecdb7dff40c3fbc9470f5907c29f74ca (0.1.0-rc.7, 2026-08-17T19:03:17+08:00) — Merge pull request #2620 from deepseek-harness/release/dsh-0.1.0-rc.7
     generated: 2026-08-17T16:41:47.200Z -->

# 合同锚点（关键字定位 + 原文摘录）

> 来源 commit `99f6f02fecdb`（版本 `0.1.0-rc.7`，提交于 2026-08-17）。本表由脚本从源码抽取；升级 harness 时重跑 `node tools/gen-facts.mjs` 并 diff。

每条锚点 = 一条 AI 常踩、且文档/源码明写的合同。列出所在文件、行号、原文摘录；升级 harness 后任一锚点缺失，生成器退出码 3。

| id | 含义 | 位置 | 原文摘录 |
|---|---|---|---|
| `A-BUNDLE-DECL` | bundle 判定：package.json 有 dsh.bundle.patch 才算可激活层 | `apps/cli/src/plugin.ts:44`  | return manifest.dsh?.bundle?.patch !== undefined |
| `A-BUNDLE-WARN` | 缺 dsh.bundle 只 warn 不报错——装了不激活 | `apps/cli/src/plugin.ts:72`  | `${NAME}: warning: ${packageName} declares no dsh.bundle — installed as a plain dependency, not a profile layer ` |
| `A-CLI-FORWARDER` | dsh plugin 是 pnpm 转发器，动词是 pnpm 的（add/remove/update），没有 install/check | `apps/cli/src/plugin.ts:3`  | * thin pnpm forwarder: initialize the profile on first use, run |
| `A-LAYER-ORDER` | 层序：bundles → profile patch → $DSH_HOME patch → --patch | `docs/user/develop/basic/publish.md:112`  | ## The loading order ⏎ The effective configuration composes over an empty root by applying, in order: ⏎ 1. Each bundle patch named in the profile's `dsh.profile.bundles` list, in list order — `@deepseek-ai/dsh-base` first, then each installed bundle in the order it was added. ⏎ 2. The profile's own `cordis.patch.yml… |
| `A-PATCH-REPLACE` | patch 整体替换 config，不深合并 | `docs/user/develop/basic/publish.md:123`  | Later layers win per row, and a patch replaces a row's entire `config` value rather than deep-merging keys. Two consequences for bundle authors: |
| `A-PATCH-ABS` | --patch 里的插件路径必须绝对 | `docs/user/develop/basic/index.md:56`  | The plugin path must be absolute. A patch file contributes configuration but does not change the profile directory from which the loader resolves module paths. |
| `A-CONFIG-SCHEMA` | Config 必须是 Schemastery schema，不能是普通对象 | `docs/user/develop/basic/config.md:45`  | When loading the plugin, Cordis uses the exported schema to validate configuration and fill defaults. Do not export a plain object as `Config`; it does not implement the Standard Schema interface required by Cordis. |
| `A-NO-DEFAULT-EXPORT` | 函数插件不得 export default（postmortem 0001） | `packages/AGENTS.md:5`  | - **Plugin exports:** service packages default-export their service class; function plugins named-export `name` / `inject` / `Config` / `apply` and have no default export. Mixing the forms makes the Loader discard the function plugin's namespace ([postmortem](../docs/postmortem/0001-acp-default-export-drops-inject.m… |
| `A-CTX-GET-OPTIONAL` | 可选服务用 ctx.get(name)，ctx.<name> 只给已声明注入 | `packages/AGENTS.md:6`  | - **Optional services use `ctx.get(name)`.** Reserve `ctx.<name>` for declared injections; the property proxy is topology-sensitive, while strict `ctx.get` reads the global service store ([postmortem](../docs/postmortem/0001-acp-default-export-drops-inject.md)). |
| `A-POSTMORTEM-0001` | 事故记录：default export 吞掉 inject/name/Config | `docs/postmortem/0001-acp-default-export-drops-inject.md:1`  | # Post-mortem 0001: ACP server crashed on connect — `export default` dropped the plugin's `inject` |
| `A-WATERFALL-NEXT` | waterfall 监听必须调 next() | `docs/user/develop/framework/events.md:66`  | Each listener may wrap the downstream result to form a processing chain. A listener **must call `next()` to delegate downstream**; omitting the call short-circuits the pipeline: |
| `A-SLOT-DECLARE` | 渲染未声明 slot / 重复声明别人的 slot → 加载期失败 | `packages/client/AGENTS.md:12`  | 2. **children = declaration + authorization**: the slots your component renders are exactly the keys of your register call's `children` object (spec values: `kind`/`scope`). Rendering a slot you didn't declare, or declaring one someone else declared, fails at load — do not work around it; the conflict is the design … |
| `A-SLOT-INJECT` | 跨包注册 slot 用 ctx.slots.inject 等待声明 | `packages/client/AGENTS.md:97`  | 4. **Registering into another package's slot**: apply order is unconstrained, and a business service is not a declaration barrier. Use `ctx.slots.inject(name, () => ctx.slots.register(...))`; it waits on the actual declaration, removes the contribution when that declaration collapses, reruns after redeclaration, and… |
| `A-CLIENT-INJECT-INFO` | dsh.client.inject 仅信息性，不排序 | `packages/client/AGENTS.md:96`  | 3. **dsh.client manifest semantics**: `platform: 'web'` always; `immediately: true` only for stage-one-prefetch infrastructure rows. `inject` lists package-name dependency edges — they are **informational only** (preflight display, HMR diffing); they do not sequence entry activation or apply order. Activation order … |
| `A-FACTORY-BANNER` | lib/client.js 必须是 factory bundle | `packages/client/tsdown.client.ts:269`  | banner: `window.__ModuleLoader__.load({ id: ${JSON.stringify(id)}, factory: (require) => {`, |
| `A-LOADED-WITHOUT-REGISTER` | 交 ESM 的运行时报错文案 | `packages/client/modules/src/client/system.ts:106`  | throw new Error(`client-modules: bundle ${url} loaded without registering "${id}" via __ModuleLoader__.load`) |
| `A-CLIENT-ROUTE` | 客户端 bundle 的服务路由 | `packages/client/modules/src/index.ts:153`  | url: `/plugins/${id}/client.js?rev=${rev}`, |
| `A-GITHUB-BUILD-CATCH` | GitHub 安装只取源码；prepare 脚本 = 授权在安装期执行代码 | `docs/user/develop/basic/publish.md:153`  | ## Installing from GitHub: the build-script catch |
| `A-SESSION-FORMAT` | 会话日志格式版本 | `packages/core/session/src/types.ts:56`  | export const SESSION_FORMAT_VERSION = 0 |
