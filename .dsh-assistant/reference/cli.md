<!-- generated by tools/gen-facts.mjs — DO NOT EDIT BY HAND
     source: DeepSeek Harness @ 528c682e061696f5a160f363f236ecbf53cbd006 (0.1.1-rc.1, 2026-08-21T14:21:44+08:00) — Merge pull request #2890 from deepseek-harness/release/dsh-0.1.1-rc.1
     generated: 2026-08-21T08:05:58.163Z -->

# dsh CLI 动词与参数

> 来源 commit `528c682e0616`（版本 `0.1.1-rc.1`，提交于 2026-08-21）。本表由脚本从源码抽取；升级 harness 时重跑 `node tools/gen-facts.mjs` 并 diff。

来源：`apps/cli/src/args.ts`、`apps/cli/src/plugin.ts`。

## 用法示例（源码 help 原文）

```
dsh --profile web                          boot the web profile (same as: dsh web)
dsh --profile headless "run the tests"     answer one task, print the result, and exit
dsh --profile tui --patch ./extra.yml      boot a custom profile with one extra overlay
dsh --profile tui --resume <session>       arguments after the launcher flags reach the app
dsh --profile web --help                   the web app's own flags and help
dsh plugin --profile tui add <package>     install a plugin into the tui profile
```

## 启动器选项

| flag | help |
|---|---|
| `--profile <name>` | the profile under $DSH_HOME/profiles to boot |
| `--patch <path>` | extra patch-list overlay applied after the profile layer (repeatable) |
| `--dump-config` | print the composed profile tree and exit |
| `--dump-default-config` | print the profile tree without its user layer or --patch overlays and exit |
| `--dump-config` | print the composed web-profile tree (with the user layer and any --patch) and exit |
| `--dump-default-config` | print the web profile's bundle layers (no user layer) and exit |

必填：`--profile <name> is required`

## `dsh plugin`（源码头注释原文）

> `dsh plugin --profile <name> <args...>` — profile plugin management as a
> thin pnpm forwarder: initialize the profile on first use, run
> `pnpm <args...>` in the profile directory, then reconcile the
> `dsh.profile.bundles` layer list against the installed state (a dependency
> resolving to a package that declares `dsh.bundle` joins the layer stack; a
> removed or bundle-less dependency leaves it). Reconciling by installed
> state, not by dependency diff, means `update` activates a package that
> gained its `dsh.bundle` declaration in a newer version.
> @module @deepseek-ai/dsh/plugin

- 因此正确写法是 `dsh plugin --profile <name> add <package-or-abs-path>` / `remove` / `update`；**没有** `dsh plugin install`、`dsh plugin check`、`dsh doctor`。
- 本地验证：`dsh --profile <name> --dump-config` 应能看到 bundle 的行。
