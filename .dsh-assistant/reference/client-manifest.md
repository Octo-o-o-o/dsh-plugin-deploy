<!-- generated by tools/gen-facts.mjs — DO NOT EDIT BY HAND
     source: DeepSeek Harness @ 99f6f02fecdb7dff40c3fbc9470f5907c29f74ca (0.1.0-rc.7, 2026-08-17T19:03:17+08:00) — Merge pull request #2620 from deepseek-harness/release/dsh-0.1.0-rc.7
     generated: 2026-08-17T16:41:47.200Z -->

# package.json `dsh.client` 声明与 `exports["./client"]`

> 来源 commit `99f6f02fecdb`（版本 `0.1.0-rc.7`，提交于 2026-08-17）。本表由脚本从源码抽取；升级 harness 时重跑 `node tools/gen-facts.mjs` 并 diff。

来源：`packages/client/modules/src/index.ts`（宿主逐字段校验，多写字段不报错也不生效）。

## 声明字段（源码原文）

```ts
interface DshClientDeclaration {
  inject?: string[]
  platform: string
  /** Boot phase-one prefetch mark; absent means lazy (fetched on demand). */
  immediately?: boolean
}
```

## 宿主的校验报错（原文，出现即说明字段写错）

| 行 | 报错 |
|---|---|
| 112 | `client-modules: ${pkgName} has a non-object dsh.client declaration` |
| 116 | `client-modules: ${pkgName} dsh.client.platform must be a string` |
| 119 | `client-modules: ${pkgName} dsh.client.inject must be a string array` |
| 122 | `client-modules: ${pkgName} dsh.client.immediately must be a boolean` |
| 141 | `client-modules: ${pkgName} exports["./client"] must be a string or an object with a string default` |
| 210 | `'client-modules: ctx.baseUrl is unset — the node half needs the config-tree anchor to resolve plugin packages'` |
| 356 | `client-modules: ${pkgName} declares dsh.client but exports no "./client" bundle` |

## 服务路由

- `url: `/plugins/${id}/client.js?rev=${rev}`,` （packages/client/modules/src/index.ts:153）
- 语义注记（`packages/client/AGENTS.md`）：`platform: 'web'` 恒定；`immediately: true` 只给一阶段预取的基础设施行；`inject` **仅信息性**（预检展示 / HMR 差分），不排序激活、不替代客户端模块导出的服务级 `inject`。
