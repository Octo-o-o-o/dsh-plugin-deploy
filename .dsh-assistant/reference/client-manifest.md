<!-- generated by tools/gen-facts.mjs — DO NOT EDIT BY HAND
     source: DeepSeek Harness @ 528c682e061696f5a160f363f236ecbf53cbd006 (0.1.1-rc.1, 2026-08-21T14:21:44+08:00) — Merge pull request #2890 from deepseek-harness/release/dsh-0.1.1-rc.1
     generated: 2026-08-21T08:05:58.163Z -->

# package.json `dsh.client` 声明与 `exports["./client"]`

> 来源 commit `528c682e0616`（版本 `0.1.1-rc.1`，提交于 2026-08-21）。本表由脚本从源码抽取；升级 harness 时重跑 `node tools/gen-facts.mjs` 并 diff。

来源：`packages/client/modules/src/index.ts`（宿主逐字段校验，多写字段不报错也不生效）。

## 声明字段（源码原文）

```ts
interface DshClientDeclaration {
  inject?: string[]
  platform: string
  /** Boot phase-one prefetch mark; absent means lazy (fetched on demand). */
  immediately?: boolean
  /**
   * Exact module-table requests beyond the implicit client baseline. Any
   * specifier is valid, including subpaths such as `<pkg>/client`; each
   * importing package declares its own exceptional requests. A type-only
   * import is not a request because the transform erases it before resolution.
   * Absent means the package uses only the baseline externals.
   */
  external?: string[]
}
```

## 宿主的校验报错（原文，出现即说明字段写错）

| 行 | 报错 |
|---|---|
| 129 | `client-modules: ${pkgName} has a non-object dsh.client declaration` |
| 133 | `client-modules: ${pkgName} dsh.client.platform must be a string` |
| 138 | `client-modules: ${pkgName} dsh.client.immediately must be a boolean` |
| 158 | `client-modules: ${pkgName} exports["./client"] must be a string or an object with a string default` |
| 250 | `if(this.mode!=="queue")throw new Error("client-modules: window.__ModuleLoader__.create called after module-system boot"` |
| 253 | `if(registration===undefined)throw new Error("client-modules: HTML did not preload ${CLIENT_MODULES_ID}/client.js"` |
| 256 | `'client-modules: ${CLIENT_MODULES_ID}/client.js requested external "'+specifier+'" before the module system existed'` |
| 259 | `"client-modules: ${CLIENT_MODULES_ID}/client.js did not export the bootstrap module face"` |
| 308 | `'client-modules: ctx.baseUrl is unset — the node half needs the config-tree anchor to resolve plugin packages'` |
| 453 | `client-modules: ${pkgName} declares dsh.client but exports no "./client" bundle` |

## 服务路由

- `url: `/plugins/${id}/client.js?rev=${rev}`,` （packages/client/modules/src/index.ts:170）
- 语义注记（`packages/client/AGENTS.md`）：`platform: 'web'` 恒定；`immediately: true` 只给一阶段预取的基础设施行；`inject` **仅信息性**（预检展示 / HMR 差分），不排序激活、不替代客户端模块导出的服务级 `inject`。
