<!-- generated by tools/gen-facts.mjs — DO NOT EDIT BY HAND
     source: DeepSeek Harness @ 76fda729799fe9b3848dbe2c211d4b231032b81e (0.1.2-rc.1, 2026-09-03T14:02:58+08:00) — Merge pull request #3481 from deepseek-harness/fix/http-proxy-rc-version
     generated: 2026-09-04T05:55:02.093Z -->

# package.json `dsh.client` 声明与 `exports["./client"]`

> 来源 commit `76fda729799f`（版本 `0.1.2-rc.1`，提交于 2026-09-03）。本表由脚本从源码抽取；升级 harness 时重跑 `node tools/gen-facts.mjs` 并 diff。

来源：`packages/client/modules/src/index.ts`（宿主逐字段校验，多写字段不报错也不生效）。

## 声明字段（源码原文）

```ts
interface DshClientDeclaration {
  inject?: string[]
  platform: string
  /** Boot phase-one registration barrier; absent rows still ride the shared application batch. */
  immediately?: boolean
  /**
   * Exact module-table requests beyond the implicit client baseline. Any
   * specifier is valid, including subpaths such as `<pkg>/client`; each
   * importing package declares its own exceptional requests. A type-only
   * import is not a request because the transform erases it before resolution.
   * Absent means the package uses only the baseline externals.
   */
  external?: string[]
}
```

## 宿主的校验报错（原文，出现即说明字段写错）

| 行 | 报错 |
|---|---|
| 204 | `client-modules: ${pkgName} has a non-object dsh.client declaration` |
| 208 | `client-modules: ${pkgName} dsh.client.platform must be a string` |
| 213 | `client-modules: ${pkgName} dsh.client.immediately must be a boolean` |
| 233 | `client-modules: ${pkgName} exports["./client"] must be a string or an object with a string default` |
| 338 | `client-modules: ${clientPath}.map is not a regular Source Map v3 object` |
| 354 | `if (original === undefined) throw new Error(`client-modules: source map missing for ${record.entry.id}` |
| 498 | `if(this.mode!=="queue")throw new Error("client-modules: window.__ModuleLoader__.create called after module-system boot"` |
| 501 | `if(registration===undefined)throw new Error("client-modules: HTML did not preload ${CLIENT_MODULES_ID}/client.js"` |
| 504 | `'client-modules: ${CLIENT_MODULES_ID}/client.js requested external "'+specifier+'" before the module system existed'` |
| 507 | `"client-modules: ${CLIENT_MODULES_ID}/client.js did not export the bootstrap module face"` |
| 762 | `client-modules: ${packageName} declares dsh.client but exports no "./client" bundle` |
| 935 | `client-modules: loader entry ${loaderName} has no resolution base URL` |

## 服务路由

- `? `/plugins/${record.entry.id}/client.js`` （packages/client/modules/src/index.ts:308）
- 语义注记（`packages/client/AGENTS.md`）：`platform: 'web'` 恒定；`immediately: true` 只给一阶段预取的基础设施行；`inject` **仅信息性**（预检展示 / HMR 差分），不排序激活、不替代客户端模块导出的服务级 `inject`。
